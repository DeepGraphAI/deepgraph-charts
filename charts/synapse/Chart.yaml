apiVersion: v2
name: synapse
description: Synapse - a graph + vector hybrid database with ISO GQL, vector search, and full-text search
type: application

# Chart version. Bump on every chart change, independent of appVersion.
version: 0.1.0

# Version of Synapse that this chart deploys by default.
appVersion: "0.1.0"

kubeVersion: ">=1.23.0-0"

home: https://github.com/deepgraphai/Synapse
sources:
  - https://github.com/deepgraphai/Synapse

keywords:
  - synapse
  - graph-database
  - vector-database
  - gql
  - iso-gql
  - opencypher
  - hybrid-search
  - knowledge-graph

annotations:
  category: Database
  licenses: Proprietary
