# chart-testing configuration.
remote: origin
target-branch: main
chart-dirs:
  - charts
validate-maintainers: false
check-version-increment: true
# The chart refuses to render without an admin password, so ct needs one for
# its lint and install passes.
helm-extra-set-args: --set=auth.password=ct-test-password
